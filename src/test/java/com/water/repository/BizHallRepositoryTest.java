/*
package com.water.repository;

import com.water.WaterCmsApplicationTests;
import com.water.domain.Area;
import com.water.domain.BizHall;
import org.junit.Assert;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;

import java.util.List;

*/
/**
 * Created by Administrator on 2017/3/26.
 *//*

public class BizHallRepositoryTest extends WaterCmsApplicationTests {

    @Autowired
    private BizHallRepository bizHallRepository;


    @Test
    public void testCreate() {
        BizHall bizHall = new BizHall();
//        bizHall.setCompan;
    }

    @Test
    public void testPageFind(){
        Pageable pageable = new PageRequest(1, 2, Sort.Direction.ASC, "id");

        Page<BizHall> data = bizHallRepository.findByCompanyId(1,pageable);
        Assert.assertEquals(0,data.getTotalElements());
    }
}
*/
